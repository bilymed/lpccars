{% extends "layouts/base.html.twig" %}
{% block head %}
    <link href="{{ app.request.basepath }}/web/css/fileinput.min.css" rel="stylesheet">
    <script  src="{{ app.request.basepath }}/web/scripts/fileinput.min.js" ></script>
    {{ parent() }}
{% endblock %}
{% block content %}
    <div class="list-voitures">
        <input id="input-1" type="file" class="file">
        <div class="container"> 
            {% for voiture in voitures %}
                <div class="row">
                    <div class="col-md-6">
                        <img src="{{ app.request.basepath }}/web/images/{{ voiture.url }}.png" alt="véhicule" width="250px" />
                    </div>
                    <div class="col-md-6">
                        <h2>{{ voiture.id }}</h2>
                        <p>{{ voiture.name }}</p>
                        <p>{{ voiture.url }}</p>
                    </div>
                </div>
                    <hr style="width: 90%; margin-right: 8%"/>
            {% endfor %}
        </div>
    </div>



{% endblock %}
